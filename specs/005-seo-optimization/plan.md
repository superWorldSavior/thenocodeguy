# Implementation Plan: SEO Optimization

**Branch**: `005-seo-optimization` | **Date**: 2026-02-26 | **Spec**: [spec.md](spec.md)
**Input**: DataForSEO API audit + wording shift to "delegation/staffing IA". All changes in 4 locales (fr, en, zh-TW, zh-CN).

## Summary

Fix all technical SEO defects (duplicate title suffix, missing H1, no external links, missing image titles), optimize meta descriptions for "agent IA" keyword targeting, create a pillar page at /agent-ia, enrich domain pages with FAQ + use cases to reach word count targets, and add SEO content to blog listing and contact pages. All changes in 4 locales.

## Technical Context

**Language/Version**: TypeScript 5 (strict mode)
**Primary Dependencies**: Next.js 16 (App Router), next-intl, Tailwind CSS v4, shadcn/ui, lucide-react
**Storage**: N/A (translation JSON files only)
**Testing**: `npm run build` (zero errors), DataForSEO crawl post-deploy
**Target Platform**: Vercel (web)
**Project Type**: web-service (marketing site)
**Performance Goals**: On-page SEO score 99+/100
**Constraints**: All changes in 4 locales (fr, en, zh-TW, zh-CN), build must pass
**Scale/Scope**: ~19 pages, 4 locale files, 1 new page, ~150 new translation keys per locale

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. User-First, Locale-First | PASS | All 4 locales covered for every change |
| II. Server Components by Default | PASS | All new pages are server components with getTranslations |
| III. Visual Quality over Speed | PASS | No visual changes to existing layout; new sections follow existing design patterns |
| IV. Confidentiality | PASS | No client names exposed; anonymized references only |
| V. Simplicity & No Over-Engineering | PASS | Translation keys + simple page components; no new dependencies |
| VI. Build Must Pass | PASS | Build verified after every implementation phase |

## Project Structure

### Documentation (this feature)

```text
specs/005-seo-optimization/
├── plan.md              # This file
├── research.md          # Root cause analysis + SEO decisions
├── data-model.md        # Translation key structure
├── quickstart.md        # Verification scenarios
├── checklists/
│   └── requirements.md  # Spec quality checklist (16/16 pass)
└── tasks.md             # Task breakdown (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
app/
├── layout.tsx                          # FIX: Remove duplicate title template
├── sitemap.ts                          # UPDATE: Add domain pages + agent-ia
└── [locale]/
    ├── layout.tsx                      # KEEP: Single source of truth for title template
    ├── agent-ia/
    │   └── page.tsx                    # NEW: Pillar page "Agent IA"
    ├── domaines/
    │   ├── btp/page.tsx                # UPDATE: Add FAQ + use cases sections
    │   ├── commerce/page.tsx           # UPDATE: Add FAQ + use cases sections
    │   ├── admin/page.tsx              # UPDATE: Add FAQ + use cases sections
    │   └── web/page.tsx                # UPDATE: Add FAQ + use cases sections
    ├── blog/page.tsx                   # UPDATE: Add SEO intro section
    └── contact/page.tsx                # UPDATE: Add contextual content

components/
└── organisms/
    ├── MissionsSection.tsx             # UPDATE: Add title to Image
    ├── DomainesSection.tsx             # UPDATE: Add title to Image
    └── HeroCarousel.tsx                # UPDATE: Add title to Image

messages/
├── fr.json                            # UPDATE: ~150 new/modified keys
├── en.json                            # UPDATE: ~150 new/modified keys
├── zh-TW.json                         # UPDATE: ~150 new/modified keys
└── zh-CN.json                         # UPDATE: ~150 new/modified keys
```

**Structure Decision**: No new directories or components needed beyond the pillar page. All enrichment is done via translation keys and inline JSX in existing page components.

## Complexity Tracking

No constitution violations. No complexity justification needed.
